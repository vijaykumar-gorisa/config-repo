server:
  port: 8082

  spring:
    datasource:
      url:
        username: admin
        password: admin

    jpa:
      hibernate:
        ddl-auto: update
        show-sql: true



  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka